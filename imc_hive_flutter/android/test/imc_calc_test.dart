
import 'package:flutter_test/flutter_test.dart';
import 'package:imc_calculator/src/models/imc.dart';

void main() {
 group('Imc', () {
    test('bmiClasifier should return the correct classification', () {
      // final imc1 = Imc(heigth: 175, width: 70); 
      // final imc2 = Imc(heigth: 160, width: 110);

      // expect(imc1.bmiClasifier(), 'Peso Ideal'); 
      // expect(imc2.bmiClasifier(), 'Obesidade Grau II'); 
    });
  });

  group('Hive test', () {
    test('TesteHive', () async{
      

    });
  });
}
